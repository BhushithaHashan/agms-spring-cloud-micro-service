spring:
  application:
    name: api-gateway
  cloud:
    config:
      discovery:
        enabled: true
        service-id: config-server 
      fail-fast: true # If it can't find the Vault, it stops immediately (cleaner)

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/