server:
  port: 8888

spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        native:
          search-locations: file:///home/bhushitha/agms-config/
  profiles:
    active: native